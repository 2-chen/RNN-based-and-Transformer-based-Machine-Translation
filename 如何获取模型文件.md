# 如何获取模型文件

## 问题说明

由于GitHub对单个文件有**100MB的限制**，所有模型文件（.pt文件）都无法直接上传到GitHub仓库。

## 解决方案

### 方案0：快速生成Demo模型（推荐用于测试）

**适用于**：快速测试、GitHub上传（文件<100MB）、演示用途

使用专门的demo训练脚本，训练小规模模型（2个epoch，小参数）：

```bash
# 训练RNN Demo模型（约30-50MB，2个epoch）
python train_demo.py --model_type rnn

# 训练Transformer Demo模型（约50-80MB，2个epoch）
python train_demo.py --model_type transformer

# 训练所有Demo模型
python train_demo.py --model_type all
```

**Demo模型配置**：
- **数据集**：train_10k.jsonl（10,000条，而非100,000条）
- **训练轮数**：2 epochs（快速训练）
- **模型规模**：
  - RNN: embed_dim=128, hidden_dim=256, num_layers=1
  - Transformer: d_model=256, n_layers=2, n_heads=4
- **预期文件大小**：<100MB（可上传GitHub）
- **性能**：仅用于测试，性能不如完整训练的模型

训练完成后，模型文件会保存在：
- `checkpoints/demo_rnn/rnn_best.pt`
- `checkpoints/demo_transformer/transformer_best.pt`

### 方案1：本地训练完整模型（推荐用于实际使用）

**适用于**：实际应用、完整实验、追求最佳性能

使用项目中的训练脚本训练完整模型：

```bash
# 训练RNN模型（jieba+NLTK版本）
python src/train_rnn.py --config configs/rnn_config.yaml

# 训练RNN模型（HanLP+BPE版本）
# 需要先修改configs/rnn_config.yaml中的save_dir为checkpoints/rnn_hanlp_bpe
python src/train_rnn.py --config configs/rnn_config.yaml

# 训练Transformer模型
python src/train_transformer.py --config configs/transformer_config.yaml

# 训练T5模型
python src/train_t5.py --config configs/t5_config.yaml
```

训练完成后，模型文件会保存在`checkpoints/`目录下。

### 方案2：使用Git LFS上传（如果有权限）

如果你有sudo权限，可以安装Git LFS来上传大文件：

```bash
# 安装Git LFS
sudo apt-get install git-lfs

# 初始化
git lfs install

# 跟踪所有.pt文件
git lfs track "checkpoints/**/*.pt"
git lfs track "checkpoints/t5_best/**"

# 添加并提交
git add .gitattributes
git add checkpoints/
git commit -m "使用Git LFS添加模型文件"
git push origin main
```

### 方案3：使用GitHub Releases

1. 将模型文件打包：
```bash
tar -czf models.tar.gz checkpoints/
```

2. 访问 https://github.com/2-chen/RNN-based-and-Transformer-based-Machine-Translation/releases
3. 创建新Release并上传 `models.tar.gz`
4. 在README中说明下载方式

### 方案4：使用云存储服务

将模型文件上传到：
- Google Drive
- Dropbox
- 百度网盘
- 阿里云OSS

然后在README中提供下载链接。

## 测试脚本行为

`test_inference.sh` 脚本会：
- ⚠️ 检测到模型文件不存在时，显示警告并跳过测试
- ✅ 这是正常行为，不影响脚本运行
- 📝 脚本会提示用户需要先训练模型或下载模型文件

## 推荐做法

**对于项目提交**：
- ✅ 上传完整的代码和配置文件
- ✅ 上传训练脚本和说明文档
- ✅ **上传Demo模型文件**（<100MB，用于快速测试）
- ❌ 不上传完整模型文件（太大，>100MB）

**对于用户使用**：

**快速测试**（推荐）：
1. 克隆仓库
2. 运行 `python train_demo.py --model_type all` 生成Demo模型（约5-10分钟）
3. 运行 `./test_inference.sh` 测试推理功能

**完整使用**：
1. 阅读README了解如何训练模型
2. 运行完整训练脚本生成模型文件（需要较长时间）
3. 使用`test_inference.sh`测试推理功能

**Demo模型 vs 完整模型**：
- **Demo模型**：快速训练（2 epochs），小参数，文件<100MB，适合测试和演示
- **完整模型**：完整训练（10-200 epochs），标准参数，文件>100MB，适合实际应用

