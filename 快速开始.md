# 快速开始指南

## 一、运行对比实验

### 最简单的方法：使用自动化脚本

```bash
# 运行所有实验（需要很长时间，建议分批运行）
python run_experiments.py

# 或者运行特定实验
python run_experiments.py rnn_attention          # RNN注意力机制对比
python run_experiments.py rnn_training           # RNN训练策略对比
python run_experiments.py transformer_pos       # Transformer位置编码对比
python run_experiments.py transformer_norm       # Transformer归一化对比
python run_experiments.py transformer_hyper     # Transformer超参数分析
```

### 手动运行单个实验示例

```bash
# RNN: 测试不同注意力机制
python src/train_rnn.py --config configs/rnn_config.yaml --attention_type dot --epochs 10
python src/train_rnn.py --config configs/rnn_config.yaml --attention_type multiplicative --epochs 10
python src/train_rnn.py --config configs/rnn_config.yaml --attention_type additive --epochs 10

# Transformer: 测试不同位置编码
python src/train_transformer.py --config configs/transformer_config.yaml --pos_encoding absolute --epochs 10
python src/train_transformer.py --config configs/transformer_config.yaml --pos_encoding relative --epochs 10
```

详细说明请查看 `实验指南.md`

---

## 二、上传到GitHub

### 步骤1：在GitHub创建仓库
1. 访问 https://github.com
2. 点击右上角 "+" -> "New repository"
3. 填写仓库名（如：`chinese-english-mt`）
4. 点击 "Create repository"

### 步骤2：在本地初始化Git

```bash
# 进入项目目录
cd "/data/nlpfinal/AP0004_Midterm&Final_translation_dataset_zh_en"

# 初始化Git
git init

# 添加文件
git add .

# 提交
git commit -m "Initial commit: 中英文机器翻译项目"

# 添加远程仓库（替换为你的GitHub用户名和仓库名）
git remote add origin https://github.com/你的用户名/chinese-english-mt.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

### 步骤3：更新报告中的链接

在 `250010008_陈嘉诚.md` 的第一页，更新：
```markdown
**代码仓库URL：** https://github.com/你的用户名/chinese-english-mt
```

详细说明请查看 `GitHub上传指南.md`

---

## 三、实验时间规划

由于完整实验需要很长时间，建议：

1. **先运行快速验证**（1个epoch）：
   ```bash
   python src/train_rnn.py --config configs/rnn_config.yaml --epochs 1
   ```

2. **分批运行实验**：
   - 第一周：RNN注意力机制对比
   - 第二周：RNN训练策略对比
   - 第三周：Transformer位置编码和归一化对比
   - 第四周：Transformer超参数分析

3. **使用小数据集测试**：
   ```bash
   # 先用10k数据快速测试
   python src/train_rnn.py --train_file data/train_10k.jsonl --epochs 5
   ```

---

## 四、常见问题

**Q: 实验时间太长怎么办？**
A: 可以减少epoch数，或使用小数据集先验证流程。

**Q: 如何查看实验结果？**
A: 查看 `checkpoints/*_results.json` 文件，或运行 `run_experiments.py` 后查看 `experiment_results/` 目录。

**Q: GitHub上传失败？**
A: 检查网络连接，确保使用Personal Access Token而不是密码。

**Q: 模型文件太大无法上传？**
A: 在 `.gitignore` 中已配置忽略模型文件，只上传代码和配置。

---

更多详细信息请查看：
- `实验指南.md` - 完整的实验说明
- `GitHub上传指南.md` - 详细的GitHub上传步骤

